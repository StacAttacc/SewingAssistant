# Required — get this from https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Optional — defaults shown below
DATABASE_URL=sqlite:////data/sewing_assistant.db
UPLOADS_DIR=/data/uploads
# Comma-separated origins for CORS; leave empty when frontend is served by the same process
CORS_ORIGINS=
